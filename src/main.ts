import { createApp } from 'vue'
import App from './App.vue'

// import { createSWRService } from './lib'

// const { mutate, useResource } = createSWRService();

// const {
//     data,
//     pending,
//     error,
// } = useResource('keya', () => true)

createApp(App).mount('#app')
